The server requires a locally running instance of MongoDB which it will store entries of wifi networks discovered.
The server also requires a running RabbitMQ server. The servers address and queue info must be updated in the file

GPS JSON Keys
position
time
mapURL
-----
error - in event of failure to lock

### App frontend ###

The front end is simple flask app. Flask serves up data from the DB to an index page built from a number of jinja2 templates.
The index page uses Crossfilter.js, D3.js and DC.js, and Leaflet.js for creating the dashboard and mapping info.
